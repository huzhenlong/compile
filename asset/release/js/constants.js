/**
 * 放常量
 * Created by zw on 2015/9/28.
 */

/**
 * 操作类型定义
 * @type {{LIST: string, ADD: string, UPDATE: string, GET: string, DELETE: string, COUNT: string, ADDNODE: string, CLEAR: string, DELETENODE: string, IMPORT: string, GETCOUNT: string, TASKLIST: string, TASKSTART: string, TASKPAUSE: string, REFRESHCOUNT: string}}
 */


MemberFilters =TxtContent.MemberFilters;

importMemberColumns =TxtContent.importMemberColumns;

WorkflowMemberColumns =TxtContent.WorkflowMemberColumns;

importTradeColumns = TxtContent.importTradeColumns;

importMenu =TxtContent.importMenu;

subsetMenu =TxtContent.subsetMenu;

workflowNodeType = {
    base: "base",
    filter: "filter",
    flow: "flow",
    marketing: "marketing",
    wait: "wait",
    oper: "oper",
    history: "history"
};


smartGroupStatus = {
    expire: 3
};


